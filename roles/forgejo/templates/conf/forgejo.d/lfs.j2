;; Ansible jinja template lfs.j2 generated from /custom/conf/app.default.ini
;; git id 12.0.0-dev-323-8b93f41aaa by update_from_default_ini.py

{% if forgejo_lfs is defined and
    forgejo_lfs | count > 0 %}

[lfs]


{% if forgejo_lfs.storage_type | default('') | string | length > 0 %}
STORAGE_TYPE = {{ forgejo_lfs.storage_type }}
{% endif %}

{% if forgejo_lfs.path | default('') | string | length > 0 %}
PATH = {{ forgejo_lfs.path }}
{% endif %}

{% if forgejo_lfs.minio_base_path | default('') | string | length > 0 %}
MINIO_BASE_PATH = {{ forgejo_lfs.minio_base_path }}
{% endif %}

;; MINIO_ENDPOINT is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_endpoint` instead

{% if forgejo_lfs.minio_endpoint | default('') | string | length > 0 %}
MINIO_ENDPOINT = {{ forgejo_lfs.minio_endpoint }}
{% endif %}

;; MINIO_ACCESS_KEY_ID is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_access_key_id` instead

{% if forgejo_lfs.minio_access_key_id | default('') | string | length > 0 %}
MINIO_ACCESS_KEY_ID = {{ forgejo_lfs.minio_access_key_id }}
{% endif %}

;; MINIO_SECRET_ACCESS_KEY is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_secret_access_key` instead

{% if forgejo_lfs.minio_secret_access_key | default('') | string | length > 0 %}
MINIO_SECRET_ACCESS_KEY = {{ forgejo_lfs.minio_secret_access_key }}
{% endif %}

;; MINIO_BUCKET is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_bucket` instead

{% if forgejo_lfs.minio_bucket | default('') | string | length > 0 %}
MINIO_BUCKET = {{ forgejo_lfs.minio_bucket }}
{% endif %}

;; MINIO_LOCATION is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_location` instead

{% if forgejo_lfs.minio_location | default('') | string | length > 0 %}
MINIO_LOCATION = {{ forgejo_lfs.minio_location }}
{% endif %}

;; MINIO_USE_SSL is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_use_ssl` instead

{% if forgejo_lfs.minio_use_ssl | default('') | string | length > 0 %}
MINIO_USE_SSL = {{ forgejo_lfs.minio_use_ssl | bodsch.core.config_bool(true_as='true', false_as='false') }}
{% endif %}

;; MINIO_INSECURE_SKIP_VERIFY is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_insecure_skip_verify` instead

{% if forgejo_lfs.minio_insecure_skip_verify | default('') | string | length > 0 %}
MINIO_INSECURE_SKIP_VERIFY = {{ forgejo_lfs.minio_insecure_skip_verify | bodsch.core.config_bool(true_as='true', false_as='false') }}
{% endif %}

;; MINIO_CHECKSUM_ALGORITHM is deprecated: Set forgejo_lfs.storage_type to a storage name like my_minio and use `storage.my_minio.minio_checksum_algorithm` instead

{% if forgejo_lfs.minio_checksum_algorithm | default('') | string | length > 0 %}
MINIO_CHECKSUM_ALGORITHM = {{ forgejo_lfs.minio_checksum_algorithm }}
{% endif %}
{% endif %}